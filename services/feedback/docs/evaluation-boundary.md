# 评测口径与证据边界

## 数据切分

- 总量 N=240。
- 开发/演示集 N=180，用于提示词、规则和聚类阈值开发。
- 锁定合成校验集 N=60，不得用于调整提示词或阈值。
- 当前 N=60 已完成 AI 辅助候选一致性复核；它不是独立人工审计，也不代表真实业务分布。

候选 v2 使用另一套全新锁定集：

- 数据版本 `v4-frozen-candidate-holdout-20260702`，N=60、30 个问题族，每族两种独立表述。
- v2 候选提示词在 v4 生成前冻结，SHA-256 为 `47bcd77f2aa5b0b472a6b2df51266875ce1329f31ca72a947555de8dddb77178`。
- v4 已完成 60/60 条 AI 辅助一致性复核及真实 Dify + BGE 回放。v4 不得用于修改候选提示词或聚类阈值；若据此调整实现，必须生成新版本锁定集。

## 展示指标

面试展示页优先使用：

- 问题类型与产品区域混淆矩阵。
- 每类 precision、recall、F1 和 support。
- 重复问题匹配精确率与召回率。
- 聚类纯度。
- 错误合并与错误拆分案例。
- 代表工单可追溯率。

B³ F1 和阈值搜索只进入技术报告。

## 质量门

| 指标 | 门槛 |
|---|---:|
| 首次 Schema 通过率 | ≥95% |
| 问题类型 Macro-F1 | ≥0.80 |
| 产品区域 Macro-F1 | ≥0.80 |
| 责任路由策略一致率 | ≥0.85 |
| 高风险升级召回率 | 100% |
| quote 自动定位成功率 | ≥95% |
| 已接受记录 offset 有效率 | 100%（硬门） |
| 重复问题匹配精确率 | ≥0.80 |
| B³ F1 | ≥0.75 |
| 周报与 SOP 引用率 | 100% |

若任一关键门槛未通过，只能声明机制已实现，不能声明分类或聚类质量达标。

## 当前事实

公开评测的权威数值见 `artifacts/evaluation/evaluation.json`。当前为真实 `dify` + BGE、数据版本 `v3-natural-singletons`：问题类型 Macro-F1 0.647、责任路由策略一致率 83.3%，未通过门槛；升级召回、quote 定位和重复问题指标通过。不得把部分通过包装成整体质量达标，也不得把 AI 辅助复核包装成独立人工审计。

候选 v2 的权威结果见 `artifacts/evaluation-v2-candidate/evaluation.json`：Schema 与 quote 定位 100%，问题类型 Macro-F1 0.954、产品区域 Macro-F1 0.943、责任路由一致率 95%、升级召回 100%，结构化门禁全部通过。聚类阈值由旧开发集确定为 0.85；该开发集同族样本实际为近重复文本，而 v4 使用独立改写，同族相似度仅 0.565–0.840，导致 30/30 个问题族被错误拆分。重复匹配精确率/召回率为 0，B³ F1 0.667，未通过门禁。

v2 因此保持 `candidate_scored_unpromoted`。不得在 v4 上调低阈值后继续把 v4 当锁定集；下一轮必须先重建包含真实改写的开发集，再生成全新未见锁定集验证。

## 四工作流套件 v5

v4 失败后，用带独立改写的开发集选出候选阈值 0.70，然后才生成全新 `v5-frozen-suite-holdout-20260702`（N=60、30 个族）。v5 manifest 在首次评分前绑定四个 DSL、聚类实现和开发集哈希。

真实 v5 回放结果：Schema 首次通过和 quote 定位均为 96.7%，问题类型 Macro-F1 0.776，产品区域 Macro-F1 0.912，责任路由一致率 78.3%，升级召回 100%；重复识别精确率 71.4%、召回率 33.3%、纯度 96.7%、B³ F1 0.785。问题类型、责任路由、重复精确率和召回率四项未达门槛。

三个内容工作流另行完成 30 次问题簇、5 次 SOP、6 次周报真实调用，成功率、证据有效率和安全动作契约均为 100%。这些通过不能抵消结构化/聚类失败，所以套件仍是 `candidate_scored_unpromoted`。v5 已不再是未见集；任何新优化都必须建立 v6。

## V6 与 V7 晋级记录

V6 使用新锁定集完成真实回放，问题类型 Macro-F1 提升到 0.901、责任路由一致率 91.7%，但重复识别精确率 57.1%、召回率 40.0%、聚类纯度 88.3%，仍未通过聚类门禁，因此保持未晋级。V6 也已成为已读评测集。

V7 在开发集上先冻结 `problem_type + product_area + issue_signature` 的阻塞策略、complete linkage、0.64 阈值与 0.2/0.8 原文/签名权重，再生成全新 `v7-frozen-signature-clustering-holdout-20260702`。manifest 在首次评分前绑定 V3 DSL、路由、聚类、向量、评测脚本及开发缓存哈希。

V7 锁定回放结果：Schema 契约有效率 100%、首次依赖成功率 88.3%、quote 定位 100%、问题类型 Macro-F1 0.846、产品区域 Macro-F1 0.963、责任路由一致率 85.0%、升级召回 100%；重复识别精确率 84.2%、召回率 53.3%、纯度 96.7%、B³ F1 0.853。九项已测门禁全部通过。

`artifacts/evaluation-v7-candidate/promotion-record.json` 将 V7 晋级为 `promoted_for_portfolio_demo`，并冻结允许的运行配置。该决定只适用于合成作品集演示；标签仅经过 AI 辅助一致性复核，不是独立人工审计，亦不证明真实业务分布、生产 SLA 或效率收益。
